// Landing page — entry point of the app (placeholder until UI step)
const Landing = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-primary font-sans text-lg">Landing Page</p>
    </div>
  );
};

export default Landing;
