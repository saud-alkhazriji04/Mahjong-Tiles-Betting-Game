// Game screen — active gameplay (placeholder until UI step)
const Game = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-primary font-sans text-lg">Game Screen</p>
    </div>
  );
};

export default Game;
