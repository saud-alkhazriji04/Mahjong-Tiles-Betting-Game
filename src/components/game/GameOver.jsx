// Game over screen — shown on win, loss, or reshuffle limit (placeholder until UI step)
const GameOver = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-primary font-sans text-lg">Game Over Screen</p>
    </div>
  );
};

export default GameOver;
